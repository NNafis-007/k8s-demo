Kubernetes local demo using minikub
- Requirements : Minikube, Kubectl
- "npm run deploy" -> runs deploy.sh -> deploys in minikube cluster -> see deployed service name
- after deploy, run "minikube service <service-name>"